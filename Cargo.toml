[package]
name = "darkbox-wm"
version = "1.0.0"
edition = "2021"
description = "Minimalistic Wayland compositor for old netbooks"

[dependencies]
smithay = "0.3"

[profile.release]
opt-level = "s"
lto = true
strip = true